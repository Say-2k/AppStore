export enum ROUTES {
  HOME = "/home",
  LIBRARY = "/library",
  PROFILE = "/profile",
  CATALOG = "/catalog",
  LOGIN = "/login",
  APP = "/app/:id",
  CART = "/cart",
  DEV = "/dev",
}

export enum Route {} // что-то
